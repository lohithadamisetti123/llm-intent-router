version: "3.9"

services:
  llm-intent-router:
    build: .
    container_name: llm-intent-router
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
    command: ["npm", "start"]
